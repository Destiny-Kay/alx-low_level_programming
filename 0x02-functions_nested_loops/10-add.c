#include "main.h"
/**
 *add- adds two numbers
 *Description:takes two integers and adds them then returns the results
 *@a: integer
 *@b: integer
 *Return: returns the sum of a and b
 */
int add(int a, int b)
{
return (a + b);
}
