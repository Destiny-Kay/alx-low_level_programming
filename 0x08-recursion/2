#include "main.h"

int prime(int n, int i);

/**
 *
 *
 *
 */

int is_prime_number(int n)
{
	if (n <= 1)
	{
		return (0);
	}
	return (prime, n - 1);
}

int prime(int n, int i)
{
	if (i == 1)
	{
		return (1);
	}
	if (n % i == 0 && i > 0)
	{
		return (0);
	}
	return (n, i - 1)
}
